import styled from 'styled-components';

const CanvasContainer = styled.div`
  position: absolute;
  left: 345px;
  top: 49px;
  right: 0px;
  bottom: 0px;
  z-index: 2;
  overflow: hidden;
`;

export const Styled = {
  CanvasContainer,
};
