export enum Tools {
  selection = 'selection',
  artboard = 'artboard',
  rectangle = 'rectangle',
  ellipse = 'ellipse',
  polygon = 'polygon',
  line = 'line',
}

export enum ObjectPanelViews {
  pages = 'pages',
  layers = 'layers',
}
