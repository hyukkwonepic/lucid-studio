export enum Tools {
  move = 'move',
  artboard = 'artboard',
  rectangle = 'rectangle',
  ellipse = 'ellipse',
  polygon = 'polygon',
  line = 'line',
}

export enum ObjectPanelViews {
  pages = 'pages',
  layers = 'layers',
}
